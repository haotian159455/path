<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC  
    "-//mybatis.org//DTD Mapper 3.0//EN"  
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<!--对应mapper接口的全路径-->
<mapper namespace="com.fh.shop.mapper.classs.IClassMapper">

    <select id="queryClass" parameterType="long" resultType="com.fh.shop.po.classs.ClassPo">
        select * from t_class_po where pid=#{id}
    </select>

</mapper>